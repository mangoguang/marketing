const state = {
  // ajaxData: {}
}

const mutations = {
  // setAjaxData: (state, obj) => state.ajaxData = obj
}

const actions = {

}

const getters = {

}

export default {
  state,
  getters,
  actions,
  mutations
}