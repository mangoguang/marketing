const state = {
 list:[]
}

const mutations = {
  setList:(state,array) => {
    state.list=list
  }
}

const actions = {
  
}

const getters = {
  
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}