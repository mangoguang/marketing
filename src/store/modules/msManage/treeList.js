const state = {
  parmas: {},
  list: []
}

const mutations = {
  setParmas: (state, obj) => state.parmas = obj,
  setList: (state, obj) => state.list = obj
}

const actions = {

}

const getters = {

}

export default {
  state,
  getters,
  actions,
  mutations
}