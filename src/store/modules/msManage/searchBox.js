const state = {
  titleList: []
}

const mutations = {
  setTitle: (state, obj) => state.title = obj
}

const actions = {

}

const getters = {

}

export default {
  state,
  getters,
  actions,
  mutations
}