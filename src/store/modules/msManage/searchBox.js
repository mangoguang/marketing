const state = {
  titleList: []
}

const mutations = {
  setTitle: (state, obj) => state.titleList = obj
}

const actions = {

}

const getters = {

}

export default {
  state,
  getters,
  actions,
  mutations
}